Виталию: учесть завершённые работы Дмитрия в README и прислать следующую задачу.

## Что сделано

- Д-8: `python scripts/check_scenario.py` — 11/11 проверок главного сценария без ключа.
- Д-10: добавлены `akim.submit()` и `akim.leaderboard()` для рейтинга команд.
- Д-11: живой агент сохраняет условия поручения, принимает числа из ответов инструментов и отклоняет выдуманные; эталон — Score 56,78, цена условий 0,46, Проверяющий срабатывает дважды.
- Д-12: для короткого структурированного разбора используется отдельная `OPENAI_EXPLAIN_MODEL=gpt-4o-mini`; основная модель агента остаётся `OPENAI_MODEL=gpt-5.6-sol`. Ответ ограничен 350 токенами.
- Исправлен путь Streamlit AppTest к корневому `app.py`; полный прогон: 76 passed, skipped нет.
- В README добавлены фактические настройки Д-12 и переносимый запуск `python -m streamlit run app.py`.

## Д-9: системный блокер Docker на ноутбуке Дмитрия

Что вызвал: `docker info` и `docker build -t akim .` после установки ядра WSL 2.

Что получил: Docker Desktop 2.5.0.0 не поднимает Linux engine; отсутствует `//./pipe/docker_engine`. Windows 10 build 19044, актуальный Docker Desktop сообщает `OS not supported`.

Что ожидал: успешную сборку, запуск контейнера и HTTP 200 на `/_stcore/health`.

Для завершения проверки нужно обновить Windows минимум до build 19045, установить актуальный Docker Desktop и повторить команды из README. Код Dockerfile не изменялся.

Пришли следующую задачу Дмитрию после обновления README.
