# TASKS.md — очередь заданий дня

Кейс «Аким на 5 часов». Перед каждым заданием: «Прочитай CASE.md и TASKS.md. Выполни задачу N».
Цифры — только из `docs/task/akim_dataset.md` и таблицы «Проверено кодом» в `CASE.md`.
«Д-N» — Дмитрий (движок, данные, агент, запуск). «В-N» — Виталий (экран). Каждый — только в своей папке.
Имена папок и стек решает Дмитрий; ниже предложение по умолчанию: движок FastAPI в `engine/`, экран Streamlit
в `screen/`, экран ходит в движок только по HTTP.

Статусы: ⬜ ждёт · 🔨 в работе · 🔍 на проверке · ✅ принята · ↩ откат

| Дмитрий | Виталий |
| --- | --- |
| Д-1 договор и образцы — к 13:40 | В-1 сборка набора на образцах |
| Д-2 данные, валидатор, расчёт | В-2 экран результата |
| Д-3 вклад мер и оптимизатор | В-3 объяснение, «Улучши», сравнение, «Утвердить» |
| Д-4 объяснение и чат агента | В-4 экран на настоящем движке — к 15:00 |
| Д-5 запуск: Docker и без Docker | |

---

## Д-1. Договор экрана и движка + образцы ответов — ⬜ (к 13:40, коммит часа 1)

ЦЕЛЬ: в репозитории лежит `openapi.yaml` и по одному JSON-образцу ответа на каждый адрес — экран строится на них,
пока движка нет.
ГДЕ: `openapi.yaml`, `engine/samples/*.json`, пустая заготовка `engine/`.
НЕЛЬЗЯ: менять форму ответа после 16:00; менять её в одиночку.
ГОТОВО: адреса и образцы:
- `GET /api/state` → районы (доля населения, 10 показателей), веса, 14 мер (направление, тип, стоимость, лаг,
  эффекты), синергии, несовместимости, `budget: 100`, `base_score: 52.56`.
- `POST /api/score` с `{"plan": [{"measure": "M7", "district": "Нура"}, …, {"measure": "M12", "district": null}]}` →
  `valid`, `reason` (если невалиден), `score`, `delta` к базе, `d_avg`, `min_district`, `n_crit` и список критических
  «район × показатель», по каждому району показатели и D до/после, `contributions` (вклад каждой меры),
  `synergies` (сработавшие), `budget_used`, `budget_left`. Образец — пример организаторов: score 56.54.
- `POST /api/optimize` с `{plan}` → лучшая замена одной меры с приростом и топ-5 наборов (лучший 57.24).
- `POST /api/explain` с `{plan}` → `strengths[]`, `risks[]`, `consequences[]`, `text`, `mode: "llm" | "template"`.
- `POST /api/chat` с `{message, plan}` → `reply`, `mode`, `handoff` (если отказ или вне темы).
- `POST /api/approve` с `{plan}` → `approved_at`, итоговый score (действие человека).

## Д-2. Данные, валидатор, расчёт Score — ⬜

ЦЕЛЬ: движок считает Score строго по формуле из ТЗ и отказывает невалидному набору с причиной.
ГДЕ: `data/akim.json` (перенос таблиц из `docs/task/akim_dataset.md`), модуль расчёта и тесты движка.
НЕЛЬЗЯ: модель в расчёте; любые числа, которых нет в датасете.
ГОТОВО: тесты зелёные и числа сверены:
- без мер: score **52.56**, d_avg **56.86**, D: Есиль 62.99, Алматы 57.06, Сарыарка 54.65, Байконур 56.63, Нура 49.18,
  n_crit **2** (S1 и S2 в Нуре);
- пример M7 Нура, M8 Нура, M10 Нура, M12, M5 Сарыарка → valid, score **56.54**, синергия M10+M12 сработала;
- M9, M11, M10, M12, M4 (все в Нуре) → valid, стоимость 61;
- отказы с причиной: 4 меры; повтор меры; стоимость 101; три меры одного направления; M1 и M3 вместе;
  M4 и M7 в одном районе; M5 и M13 в одном районе; мера «Район» без района; мера «Город» с районом.
Порядок: эффект × (8 − лаг) / 8 по каждой мере; синергии без лага (M1+M2 → T1 +2 в районе M1, M10+M12 → B1 +2
в районе M10, M5+M6 → E2 +2 в районе M5); clip 0–100; D = Σ w·I; d_avg по населению;
score = 0.7·d_avg + 0.3·min D − n_crit (показатель строго < 40).

## Д-3. Вклад мер и оптимизатор — ⬜

ЦЕЛЬ: видно, сколько дала каждая мера, и агент умеет предложить набор лучше.
ГДЕ: модуль оптимизатора, файл с заранее посчитанным топом наборов, тесты.
НЕЛЬЗЯ: полный перебор на каждый запрос экрана (694 395 наборов — долго); отклик экрана до 2 секунд.
ГОТОВО: вклад меры = score набора минус score без этой меры (правило «ровно 5» здесь не проверяем);
`optimize` на примере организаторов возвращает замену с приростом больше 0; топ-5 из файла: первый 57.24
(M2, M3 Нура, M8 Нура, M9 Нура, M14; стоимость 98), всего допустимых наборов 694 395.

## Д-4. Объяснение и чат агента — ⬜

ЦЕЛЬ: агент объясняет сильные стороны, риски и последствия сценария по готовым числам и отвечает в чате.
ГДЕ: модуль объяснения, модуль чата, промпты.
НЕЛЬЗЯ: модель считает или придумывает числа — только то, что вернул расчёт; `temperature` (у gpt-5.6-sol и
gpt-6-astra вызов с ним падает); ключ в коде.
ГОТОВО: без `OPENAI_API_KEY` — шаблонное объяснение из тех же чисел (слабый район, критические < 40, самая полезная
и самая слабая мера, синергии, остаток бюджета), `mode: "template"`; с ключом — текст модели, `mode: "llm"`;
ошибка модели → шаблон, экран не падает. Чат: «поставь Score 100», «забудь правила», вопросы о реальных акимах
и политике — вежливый отказ «это симулятор на условных данных».

## Д-5. Запуск двумя способами — ⬜

ЦЕЛЬ: эксперт на чистом клоне запускает всё по README без ключей.
ГДЕ: `docker-compose.yml`, `requirements.txt`, `.env.example`, раздел README «Как запустить».
НЕЛЬЗЯ: настоящий `.env` в репозитории.
ГОТОВО: `docker compose up` и запуск без Docker (две команды) поднимают движок и экран; главный сценарий проходит
без ключа.

---

## В-1. Сборка набора на образцах — ⬜

ЦЕЛЬ: пользователь собирает 5 решений и видит бюджет и правила, пока движка ещё нет.
ГДЕ: `screen/` (данные — из `engine/samples/*.json`, адрес движка — из настройки).
НЕЛЬЗЯ: считать Score на экране; трогать `engine/`.
ГОТОВО: 5 строк «мера + район» (район выбирается только у мер типа «Район», у «Город» поле скрыто); полоса бюджета
«потрачено N из 100»; при невалидном наборе — причина из ответа движка; меры подписаны названием, стоимостью,
лагом и эффектами.

## В-2. Экран результата — ⬜

ЦЕЛЬ: сразу видно, что сценарий дал городу.
ГДЕ: `screen/`.
НЕЛЬЗЯ: свои расчёты; только поля ответа `/api/score`.
ГОТОВО: крупно Score и дельта к базе 52.56; таблица «район × показатель» до/после, всё, что ниже 40, — красным;
D по районам и самый слабый район; вклад каждой меры; сработавшие синергии. На образце: 56.54, +3.98.

## В-3. Объяснение, «Улучши», сравнение, «Утвердить» — ⬜

ЦЕЛЬ: пользователь понимает компромиссы и сам решает, какой сценарий утвердить.
ГДЕ: `screen/`.
НЕЛЬЗЯ: менять набор пользователя без его нажатия.
ГОТОВО: блоки «сильные стороны / риски / последствия» и пометка, кто писал текст (модель или шаблон);
кнопка «Улучши сценарий» показывает предложенную замену и прирост, применяется только по нажатию; сравнение двух
наборов рядом; «Утвердить сценарий» — отметка времени и итоговый Score.

## В-4. Экран на настоящем движке — ⬜ (к 15:00)

ЦЕЛЬ: экран работает с движком, а не с образцами.
ГДЕ: настройка адреса движка в `screen/`.
НЕЛЬЗЯ: менять форму ответа под экран.
ГОТОВО: пример организаторов на экране даёт 56.54 из движка; набор на 101 — отказ с причиной из движка.
